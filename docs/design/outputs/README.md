# Design Outputs

`docs/design/outputs/` tracks user-confirmed design deliverables and sealed event
snapshots.

- `current/` records the current confirmed entry points.
- `archive/<event_id>/` contains traceable sealed snapshots.
- `index.yml` is the project-level output map.

Temporary files, build artifacts, and unconfirmed experiments are excluded.
